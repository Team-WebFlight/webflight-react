/* global describe, it */
'use strict'
const chai = require('chai')
const assert = chai.assert
const createTorrent = require('../../lib/createTorrent')
const filesSrcArray = require('../fixtures/filesArray').dirname

//describe('createTorrent', () => {
//  it('should return a string', () => {
    

process.stdout.write('files src array ', filesSrcArray)

